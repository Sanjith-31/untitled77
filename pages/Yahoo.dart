import 'package:flutter/material.dart';

class Yahoo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Yahoo')),
      body: Center(child: Text('Content of Yahoo')),
    );
  }
}